// author: Will Tatum
// tests arithmetic precendence

main
{
    int a, b, c;
    bool t;

    a = 1 + 2 * 3;        // 7
    b = (1 + 2) * 1;      // 3
    t = not (false or false) and true; // true

    call printInt(a); call println();
    call printInt(b); call println();
    call printBool(t); call println();
}.
